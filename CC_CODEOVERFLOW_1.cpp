#include<stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t,j,i,n,k,s;
    cin>>t;
    while(t--)
    {
        cin>>n>>k>>s;
        int a[n];
        for(i=0;i<n;i++)
        {
            cin>>a[i];
        }
        for(i=k+1;i<n;i++)
        {
            for(j=0;j<n;j++)
            {

            }
        }
    }
}
