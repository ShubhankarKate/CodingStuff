#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t=1,i,l,l1,l2;
    string s, s1, s2, temp;
    while(t--)
    {
        fin=0;
        cin>>s;
        cin>>s1>>s2;
        l=s.length();
        l1=s1.length();
        l2=s2.length();
        for(i=0;i<l-l1+1;i++)
        {
            temp = 1;
        }
    }
}
